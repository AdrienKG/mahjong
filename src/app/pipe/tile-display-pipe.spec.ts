import { TileDisplayPipe } from './tile-display-pipe';

describe('TileDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new TileDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
