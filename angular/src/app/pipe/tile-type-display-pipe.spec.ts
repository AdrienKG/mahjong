import { TileTypeDisplayPipe } from './tile-type-display-pipe';

describe('TileTypeDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new TileTypeDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
