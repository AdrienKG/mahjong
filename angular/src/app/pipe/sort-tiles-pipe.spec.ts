import { SortTilesPipe } from './sort-tiles-pipe';

describe('SortTilesPipe', () => {
  it('create an instance', () => {
    const pipe = new SortTilesPipe();
    expect(pipe).toBeTruthy();
  });
});
