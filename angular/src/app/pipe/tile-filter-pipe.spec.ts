import { TileFilterPipe } from './tile-filter-pipe';

describe('TileFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TileFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
