import { WindTypeDisplayPipe } from './wind-type-display-pipe';

describe('WindTypeDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new WindTypeDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
